#include <stdio.h>

#include "Factorial.h"

int main()

	{
		
		int a ;
		scanf("%d",&a) ;
		printf("factorial %d =%d\n ",a,Factorial(a));
		scanf("\n%d",&a) ;
		printf("factorial %d =%d",a,Factorial(a));
		return 0;
	}
		
	
